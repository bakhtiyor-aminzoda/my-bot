# 1. Ignore everything in the root directory
/*

# 2. Allow Bot Directory (and everything inside it)
!/bot/

# 3. Allow Deployment Files in Root
!/Dockerfile
!/docker-compose.yml
!/requirements.txt
!/.gitignore

# 4. Explicitly Block Secrets inside allowed folders
/bot/credentials.json
/bot/__pycache__/
/bot/.env
